# How do set types differ from sequence types? 

# Sequence types are ordered, set types are unordered --> sequence can be indexed, sets cannot 
# Sets have members that are unique, sequence can have duplicates 
